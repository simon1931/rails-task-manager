<h1><%= @task.title %></h1>

<p>
  <%= @task.details %>
  <br>
  <input type="checkbox" <%= "checked" if @task.completed %>>
  <br>
</p>

<%= link_to "Edit this task", edit_tasks_path(@task) %>
<br>
<%= link_to "Back to Tasks", tasks_path %>
<br>
<%= link_to "🗑", task_path(@task), method: :delete, data: { confirm: "Are you sure?" } %>
